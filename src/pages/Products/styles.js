import styled from "styled-components";

export const Container = styled.div`
  .input-component {
    max-width: 500px;
    margin-inline: auto;
  }

  ul {
    list-style: none;
    margin-top: 2.4rem;
    li + li {
      margin-top: 1rem;
    }
  }
`;
